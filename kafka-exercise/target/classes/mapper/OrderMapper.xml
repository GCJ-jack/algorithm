<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaojun.Mapper.OrderMapper">

    <!-- 测试用 -->
    <select id="ping" resultType="int">
        select 1
    </select>

    <!-- 插入订单 -->
    <insert id="insert" parameterType="com.chaojun.DTO.Order">
        INSERT IGNORE INTO t_order (order_id, user_id, amount)
        VALUES (#{orderId}, #{userId}, #{amount})
    </insert>

</mapper>
